package com.bookmarkapp.queuemark.ui.theme

import androidx.compose.ui.graphics.Color

// Design tokens from .ai/DESIGN.md — do not use raw hex in composables,
// always go through MaterialTheme.colorScheme.

// Primary
val SlateTeal = Color(0xFF2B5C55)
val MutedSage = Color(0xFF89D3C4)

// Secondary
val OliveGray = Color(0xFF5F6E65)
val SageGray = Color(0xFFA5B4A8)

// Background
val OffWhite = Color(0xFFF8F9F8)
val DarkCharcoal = Color(0xFF1A1D1A)

// Surface
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF222622)

// Accent — mapped to tertiary; used for Quick Wins and urgent reminders
val Amber = Color(0xFFD97706)
val AmberLight = Color(0xFFF59E0B)
